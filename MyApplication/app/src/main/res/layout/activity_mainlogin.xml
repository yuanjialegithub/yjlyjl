<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:padding="5dp">

    <include layout="@layout/titlelogin"></include>

    <Button
        android:id="@+id/sbt_1_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="开始计时"
        android:textColor="#000"
        android:textSize="15sp"
        android:background="@drawable/bg_btn1">
    </Button>

    <Button
        android:id="@+id/sbt_2_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/sbt_1_login"
        android:text="开始计时"
        android:textColor="#000"
        android:textSize="15sp"
        android:layout_marginTop="10dp"
        android:background="@drawable/bg_btn1">
    </Button>

    <Button
        android:id="@+id/sbt_3_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/sbt_2_login"
        android:text="开始计时"
        android:textColor="#000"
        android:textSize="15sp"
        android:layout_marginTop="10dp"
        android:background="@drawable/bg_btn1">
    </Button>

    <Button
        android:id="@+id/sbt_4_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/sbt_3_login"
        android:text="开始计时"
        android:textColor="#000"
        android:textSize="15sp"
        android:layout_marginTop="10dp"
        android:background="@drawable/bg_btn1">
    </Button>

    <Button
        android:id="@+id/sbt_5_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/sbt_4_login"
        android:text="开始计时"
        android:textColor="#000"
        android:textSize="15sp"
        android:layout_marginTop="10dp"
        android:background="@drawable/bg_btn1">
    </Button>
</RelativeLayout>